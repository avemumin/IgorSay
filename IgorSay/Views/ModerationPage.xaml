<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="IgorSay.Views.ModerationPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="ModerationPage"
    BackgroundColor="Black">

    <CollectionView ItemsSource="{Binding PendingTerms}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Grid
                    Padding="10"
                    ColumnDefinitions="2*,3*,Auto,Auto"
                    RowDefinitions="Auto">
                    <!--  Key  -->
                    <Label
                        Grid.Column="0"
                        FontAttributes="Bold"
                        Text="{Binding Key}"
                        TextColor="LimeGreen"
                        VerticalOptions="Center" />

                    <!--  Value  -->
                    <Label
                        Grid.Column="1"
                        Text="{Binding Value}"
                        TextColor="White"
                        VerticalOptions="Center" />

                    <!--  Akceptuj  -->
                    <Button
                        Grid.Column="2"
                        BackgroundColor="Green"
                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ApproveCommand}"
                        CommandParameter="{Binding .}"
                        Text="✔"
                        TextColor="White" />

                    <!--  Odrzuć  -->
                    <Button
                        Grid.Column="3"
                        BackgroundColor="Red"
                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RejectCommand}"
                        CommandParameter="{Binding .}"
                        Text="✖"
                        TextColor="White" />
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
